# core
python-dotenv>=1.0
PyYAML>=6.0
pandas>=2.0
requests>=2.31
tenacity>=8.2
tldextract>=5.0
rapidfuzz>=3.0
langdetect>=1.0.9

# search providers (only SearXNG is wired by default; others behind enable flags)
ddgs>=9.0
tavily-python>=0.5
firecrawl-py>=1.0

# crawling
crawl4ai>=0.6
lxml~=5.3                      # pinned: trafilatura>=2.1 wants lxml>=6.1, but crawl4ai 0.9 requires ~=5.3
trafilatura>=2.1               # Stage 5 page-input extraction: strips nav/boilerplate before LLM truncation

# LLM + validation
openai>=1.50
instructor>=1.5
pydantic>=2.7

# dev / verification
pytest>=8.0
